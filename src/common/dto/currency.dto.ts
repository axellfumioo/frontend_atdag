export interface CreateCurrencyDto {
    code: string;
    name: string;
    order: number;
    symbol: string;
}

export interface UpdateCurrencyDto {
    code: string;
    name: string;
    order: number;
    symbol: string;
}